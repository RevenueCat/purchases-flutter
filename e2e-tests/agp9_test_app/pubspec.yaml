name: agp9_test_app
description: "Build-only fixture exercising purchases_ui_flutter under AGP 9."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0
  flutter: '>=3.44.0'

dependencies:
  flutter:
    sdk: flutter
  purchases_flutter:
    path: ../../
  purchases_ui_flutter:
    path: ../../purchases_ui_flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

dependency_overrides:
  purchases_flutter:
    path: ../../

flutter:
  uses-material-design: true
