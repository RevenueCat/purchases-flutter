# JDK 21 and Gradle 9.1 are required by AGP 9.
# This .sdkmanrc is consumed by `sdk env install` in the agp9-smoke-test
# CircleCI job. Every other job takes JDK 17 from mise.toml; only this smoke
# test needs JDK 21 + a newer Gradle.
java=21.0.7-librca
gradle=9.1.0
